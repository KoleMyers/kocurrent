# Copy to .env and fill in. Used by Rocket.Chat, MongoDB, Jitsi, and Caddy.
# Rocket.Chat Jitsi app: Domain = meet.kocurrent.com, Use SSL = on.
# Caddy: edit Caddyfile so domains match ROOT_URL and PUBLIC_URL below.

# =============================================================================
# Rocket.Chat
# =============================================================================
ROOT_URL=https://chat.kocurrent.com
RELEASE=8.0.1
ROCKETCHAT_HOST_PORT=3000
ROCKETCHAT_IMAGE=registry.rocket.chat/rocketchat/rocket.chat

# Optional: memory limit (default 2g). Keep under 8GB total with Jitsi.
# ROCKETCHAT_MEMORY_LIMIT=2g

# =============================================================================
# MongoDB (Rocket.Chat)
# =============================================================================
MONGODB_VERSION=8.2
MONGODB_REPLICA_SET_NAME=rs0

# Optional: memory limit (default 1536m).
# MONGODB_MEMORY_LIMIT=1536m

# =============================================================================
# Caddy (optional memory limit; default 128m)
# =============================================================================
# CADDY_MEMORY_LIMIT=128m

# =============================================================================
# Jitsi – config directory (bind-mounted)
# =============================================================================
CONFIG=./jitsi-cfg

# =============================================================================
# Jitsi – public URL (required)
# Set to your Jitsi domain; Rocket.Chat Jitsi app uses this as “Domain”.
# =============================================================================
PUBLIC_URL=https://meet.kocurrent.com

# =============================================================================
# Jitsi – timezone
# =============================================================================
TZ=UTC

# =============================================================================
# Jitsi – XMPP (defaults work when PUBLIC_URL is set)
# =============================================================================
XMPP_DOMAIN=meet.jitsi
XMPP_SERVER=xmpp.meet.jitsi
XMPP_AUTH_DOMAIN=auth.meet.jitsi
XMPP_MUC_DOMAIN=muc.meet.jitsi
XMPP_INTERNAL_MUC_DOMAIN=internal-muc.meet.jitsi
XMPP_GUEST_DOMAIN=guest.meet.jitsi
XMPP_RECORDER_DOMAIN=recorder.meet.jitsi

# =============================================================================
# Jitsi – auth (required). Set strong random passwords.
# Generate with: openssl rand -hex 16
# =============================================================================
JICOFO_AUTH_PASSWORD=
JVB_AUTH_PASSWORD=

# Optional if you add Jigasi/Jibri later (not used on 8GB stack)
JIGASI_XMPP_PASSWORD=
JIGASI_TRANSCRIBER_PASSWORD=
JIBRI_RECORDER_PASSWORD=
JIBRI_XMPP_PASSWORD=

# =============================================================================
# Jitsi – JVB (video bridge). Required for NAT / public access.
# =============================================================================
JVB_PORT=10000
JVB_COLIBRI_PORT=8080
# Set to your server’s public IP or hostname (e.g. for Hetzner VPS).
DOCKER_HOST_ADDRESS=
# Optional: comma-separated list if different from DOCKER_HOST_ADDRESS.
# JVB_ADVERTISE_IPS=

# =============================================================================
# Jitsi – web / auth
# =============================================================================
AUTH_TYPE=internal
ENABLE_AUTH=0
ENABLE_GUESTS=1

# =============================================================================
# Jitsi – HTTPS (Caddy handles TLS; leave these off so Jitsi listens on HTTP only)
# =============================================================================
# ENABLE_LETSENCRYPT=0
# DISABLE_HTTPS=1

# =============================================================================
# Jitsi – optional memory limits (defaults total ~4GB for Jitsi)
# Tune if you need headroom on 8GB (e.g. lower JVB for 720p-only).
# =============================================================================
# JITSI_WEB_MEMORY_LIMIT=256m
# JITSI_PROSODY_MEMORY_LIMIT=256m
# JICOFO_MEMORY_LIMIT=512m
# JVB_MEMORY_LIMIT=3g

# =============================================================================
# Jitsi – image tag
# =============================================================================
JITSI_IMAGE_VERSION=stable
