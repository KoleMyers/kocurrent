# Copy to .env and fill in. Required for Jitsi; TeamSpeak uses only TS3SERVER_LICENSE (set in compose).

# --- Jitsi: config directory (bind-mounted) ---
CONFIG=./jitsi-cfg

# --- Jitsi: ports ---
HTTP_PORT=80
HTTPS_PORT=443

# --- Jitsi: public URL (required). Use your domain or VPS IP, e.g. https://meet.example.com ---
PUBLIC_URL=https://meet.example.com

# --- Jitsi: timezone ---
TZ=UTC

# --- Jitsi: XMPP (defaults often work if PUBLIC_URL is set) ---
XMPP_DOMAIN=meet.jitsi
XMPP_SERVER=xmpp.meet.jitsi
XMPP_AUTH_DOMAIN=auth.meet.jitsi
XMPP_MUC_DOMAIN=muc.meet.jitsi
XMPP_INTERNAL_MUC_DOMAIN=internal-muc.meet.jitsi
XMPP_GUEST_DOMAIN=guest.meet.jitsi
XMPP_RECORDER_DOMAIN=recorder.meet.jitsi

# --- Jitsi: auth (required – set strong random passwords) ---
# Generate with: openssl rand -hex 16
JICOFO_AUTH_PASSWORD=
JVB_AUTH_PASSWORD=

# Optional: Jigasi/Jibri if you add those later
JIGASI_XMPP_PASSWORD=
JIGASI_TRANSCRIBER_PASSWORD=
JIBRI_RECORDER_PASSWORD=
JIBRI_XMPP_PASSWORD=

# --- Jitsi: JVB (video bridge). Set DOCKER_HOST_ADDRESS to your server's public IP or hostname for NAT ---
JVB_PORT=10000
JVB_COLIBRI_PORT=8080
# DOCKER_HOST_ADDRESS=your.vps.ip.or.hostname
# JVB_ADVERTISE_IPS=your.vps.ip

# --- Jitsi: web env (for nginx/config) ---
AUTH_TYPE=internal
ENABLE_AUTH=0
ENABLE_GUESTS=1

# --- Optional: HTTPS with Let's Encrypt ---
# ENABLE_LETSENCRYPT=1
# LETSENCRYPT_DOMAIN=meet.example.com
# LETSENCRYPT_EMAIL=you@example.com

# --- Image tag (unstable = latest from Jitsi) ---
JITSI_IMAGE_VERSION=unstable
